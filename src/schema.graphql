# import Link from "./generated/prisma.graphql"

type Query {
  info: String!
  feed: [Link!]!
  link(id: ID!): Link
}

type Mutation {
  post(url: String!, description: String!): Link!
  post(url: String!, password: String!, name: String!): AuthPayload
  login(email: String!, password: String!): AuthPayload
}

type AuthPayload {
  token: String
  user: user
}

type User {
  id: ID!
  name: String!
  email: String!
  links: [Link!]!
}
